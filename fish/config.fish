set EDITOR /usr/bin/vim
set fish_greeting ""
set -x PATH $PATH:/usr/local/share/python:/usr/local/bin:/usr/local/mysql/bin

git config --global core.editor "vim"
git config --global user.name "Jesper Petersson"
git config --global user.email jesper@jpetersson.se
